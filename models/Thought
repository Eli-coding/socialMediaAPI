const { Schema, model } = require("mongoose");
const reactionsM = requiere ("./Reaction")

const thoughts = new Schema({
  thoughtText: {
    type: String,
    required: true,
    maxlength: 280,
  },

  createdAt: {
    type:Date, //not sure 
    timestamp: true,
  },
  username: {
    type: String,
    required: true,
  },

  reactions: [reactionM]
});

thoughts.virtual("friendCount").get(function(){
    return this.friends.length 

const Thought = model('Thought', 'thoughts');
module.exports = Thought; 