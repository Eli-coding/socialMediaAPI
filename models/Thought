const { Schema, model } = require("mongoose");

const thoughts = new Schema({
  thoughtText: {
    type: String,
    required: true,
    maxlength: 280,
  },

  createdAt: {
    type:Date, //not sure 
    timestamp: true,
  },
  username: {
    type: String,
    required: true,
  },
});
